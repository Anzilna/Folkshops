import { ComingSoon } from "../coming-soon";

export default function SettingsPage() {
  return (
    <ComingSoon
      title="Settings"
      description="Store settings and team invitations aren't built yet — staff invitations are explicitly deferred for now."
    />
  );
}
