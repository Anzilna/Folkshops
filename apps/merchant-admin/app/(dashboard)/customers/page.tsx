import { ComingSoon } from "../coming-soon";

export default function CustomersPage() {
  return (
    <ComingSoon
      title="Customers"
      description="Storefront customer accounts exist in the backend (phone + OTP login) — this view isn't built yet."
    />
  );
}
