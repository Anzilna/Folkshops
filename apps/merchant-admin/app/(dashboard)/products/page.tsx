import { ComingSoon } from "../coming-soon";

export default function ProductsPage() {
  return (
    <ComingSoon
      title="Products"
      description="The products API is fully built and tested — this management page isn't wired up yet."
    />
  );
}
