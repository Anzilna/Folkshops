import { ComingSoon } from "../coming-soon";

export default function TenantsPage() {
  return (
    <ComingSoon
      title="Tenants"
      description="A cross-tenant view for internal staff isn't built yet — the tenants table itself exists and is used by every store's own auth already."
    />
  );
}
