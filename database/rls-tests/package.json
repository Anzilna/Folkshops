{
  "name": "@folkshops/rls-tests",
  "version": "0.0.0",
  "private": true,
  "description": "Mandatory concurrent Row-Level Security isolation tests. Connects directly to Postgres with raw pg as the app's own non-superuser runtime role — proves the database enforces isolation, independent of application code correctness.",
  "scripts": {
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^22.0.0",
    "@types/pg": "^8.11.0",
    "jest": "^29.7.0",
    "pg": "^8.13.0",
    "ts-jest": "^29.2.0",
    "typescript": "^5.6.0"
  }
}
